registered_models:
  - resnet-atlas

processing_queues:
  resnet-atlas: 
    grid.registered: resnet_atlas_infer

distributed:
  resnet-atlas: 
    grid.registered: false

model_parameters:
  resnet-atlas:
    grid.registered:
      model_path: "/path/to/weights"
